pre-commit:
  parallel: true
  jobs:
    - run: bunx prettier --write --list-different --cache {staged_files}
    - run: bunx eslint --no-warn-ignored --fix {staged_files}
      glob: "*.{js,ts,jsx,tsx,mjs,cjs,json,jsonc}"
    - run: bunx tsc --noEmit
      glob: "*.{ts,tsx}"
