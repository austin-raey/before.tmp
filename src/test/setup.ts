/**
 * NOTE:
 * You may wish to simply have a single global mocks file - as opposed to a directory -
 * to keep everything in one place.
 *
 * Alternatively, keeping mocks in a directory with seperate files may help with organization.
 */
import "./mocks";
